services:
  moontracer:
    build: .
    image: moontracer:latest
    container_name: moontracer
    restart: unless-stopped
    secrets:
      - discord_bot_token
      - discord_guild_id
    volumes:
      - /mnt/bladesinger/moontracer:/app/data
    networks:
      - caddy_proxy

networks:
  caddy_proxy:
    external: true

secrets:
  discord_bot_token:
    file: /mnt/bladesinger/moontracer/secrets/discord_bot_token.txt
  discord_guild_id:
    file: /mnt/bladesinger/moontracer/secrets/discord_guild_id.txt

